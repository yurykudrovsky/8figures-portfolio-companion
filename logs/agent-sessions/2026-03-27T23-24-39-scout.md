# Agent Session — SCOUT — 2026-03-27T23-24-39

## Agent
scout

## Task
perform full codebase audit per your instructions, read all files in frontend/src/app recursively, output audit report to audit-reports/scout-audit-002.md

## Started
2026-03-27T22:24:39Z

## Status
running

---

I need write permission to create the audit report file. Could you grant permission to write to `/Users/prime/Documents/dev/ai/8figures/audit-reports/2026-03-27-scout-audit-002.md`?

In the meantime, here is the complete audit summary from the scan:

---

## Audit Results — Full Frontend Codebase (2026-03-27)

**Build:** ✓ Clean — 0 errors  
**Tests:** ✓ 27/27 passing  
**`any` types:** 0  **`console.log`:** 0  **`NgModule`:** 0

---

### CRITICAL (0)
*None found.*

---

### WARNINGS (6)

**W1 — `app.ts:4` — Root `App` component missing `ChangeDetectionStrategy.OnPush`**  
Both feature components have `OnPush`; the root does not, making it the weak link in the change-detection chain.

**W2 — `core/models/portfolio.model.ts:24-35` — Chat models (`ChatMessage`, `ChatContext`) leaked into the portfolio model file**  
These interfaces are chat-feature domain objects and are already correctly defined in `features/chat/models/chat.model.ts`. The definitions in `portfolio.model.ts` are orphaned duplicates that no component imports from that path. Violates the feature-based folder structure rule.

**W3 — `chat-page.component.html:49` — `$any($event.target)` bypasses strict template type-checking**  
`$any()` is the template equivalent of casting to `any` and directly violates the "No implicit any — EVER" rule. The correct type is `HTMLIonTextareaElement` or reading `$event.detail.value` from the typed Ionic event.

**W4 — `portfolio-dashboard.component.ts:137-160` — Business logic (`formatCurrency`, `formatPercent`, `formatChange`, `assetTypeLabel`) lives on the component**  
CLAUDE.md mandates: "Services handle ALL business logic — components are display only." These four methods should be in a service or Angular pipe.

**W5 — `portfolio-dashboard.component.ts:116-135` — `handleRefresh` creates a new unguarded subscription on every pull-to-refresh**  
`takeUntilDestroyed` only cancels on component destroy, not on the next refresh. Rapid pulls create multiple concurrent HTTP requests; whichever resolves last overwrites the others — a silent race condition.

**W6 — `frontend/src/app/` — `shared/` folder is completely absent**  
CLAUDE.md explicitly defines `app/shared/` as a required layer. No directory or files exist there.

---

### INFO (5)

**I1** — `POST /api/chat` (CLAUDE.md API spec) is never called; all AI responses are generated client-side via `setInterval`. Intentional for the mock, but backend spec and frontend are permanently disconnected.

**I2** — `loadPortfolio()` and `handleRefresh()` contain near-identical subscription blocks (duplication will drift).

**I3** — `handleRefresh(event: CustomEvent)` should be typed as `IonRefresherCustomEvent<RefresherEventDetail>` to eliminate the `as HTMLIonRefresherElement` cast.

**I4** — `ChatPageComponent` is a monolithic smart+dumb component; message bubbles, input bar, and typing indicator should be extracted as dumb sub-components per the Smart/Dumb pattern.

**I5** — Currency/percentage formatting helpers are duplicated between `ChatService` and `PortfolioDashboardComponent` using identical logic — will diverge on locale changes.

---

**Recommended next step for ARCHITECT:** Design a shared `PortfolioFormatterService` (or Angular standalone pipes) centralising all formatting logic, and define a `switchMap`-based `loadPortfolio$` stream in `PortfolioService` to eliminate the pull-to-refresh race condition and the duplicated subscription code in the dashboard component.

Once write permission is granted I will output the file and confirm `SCOUT COMPLETE`.

---

## Completed
2026-03-27T22:28:51Z

## Status
complete
